version: '3.8'

services:
  blog:
    image: node:20.12.2
    working_dir: /app
    volumes:
      - ./app:/app
    ports:
      - "8080:8080"
    user: "${UID:-1000}:${GID:-1000}"
    command: make dev